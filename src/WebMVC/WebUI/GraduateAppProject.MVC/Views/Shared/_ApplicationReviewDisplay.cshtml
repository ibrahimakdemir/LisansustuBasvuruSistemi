@model GraduateProgram
@{
    var userId = ViewData["UserId"];
    var reasons = ViewData["ReasonsList"];
}
@*<form method="post" asp-action="EvaluateApplication" asp-controller="Admin">
    <div class="container m-2">
        <div class="rounded d-flex justify-content-center">
            <div class="col-lg-8 col-12 shadow-lg bg-white rounded rounded-3">
                <h3 class="text-center text-success fw-bold mt-4">Başvuru Yapılan Program Detayları</h3>
                <partial name="_GetGraduateProgramDetails" model="@Model"></partial>

                <div class="form-group mt-4">
                    <label for="reasonDropdown">Reddetme Nedeni:</label>
                    <select class="form-control" id="reasonDropdown" name="ReasonId">
                        <option value="">Seçiniz</option>
                        @foreach (var reason in @reasons)
                        {
                            <option value="@reason.Id">@reason.Name</option>
                        }
                    </select>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-danger" name="applicationStateId" value="2">Reddet</button>
                    <button type="submit" class="btn btn-success" name="applicationStateId" value="1">Onayla</button>
                </div>
            </div>
        </div>
    </div>
</form>*@