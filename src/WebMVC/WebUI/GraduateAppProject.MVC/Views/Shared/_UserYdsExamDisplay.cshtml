@model IEnumerable<UserYdsExamDTO>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="container" style="margin-top:100px">
    <div class="rounded d-flex justify-content-center">
        <div class="col-lg-8 col-12 shadow-lg bg-white rounded rounded-3">
            <div class="text-center">
                <h3 class="text-success">YDS Sınav Sonuçları</h3>
                <div class="p-2">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    Sınav Sonuç Belgesi
                                </th>
                                <th>
                                    YDS Puanı
                                </th>
                                <th>
                                    Sınav Yılı
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <button class="btn btn-sm btn-success documentBtn" data-url="@item.DocumentUrl">Göster</button>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.YdsGrade)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Year)
                                    </td>
                                    @*<td>
                                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                                </td>*@
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
</div>

